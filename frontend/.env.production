# 页面标题
VITE_APP_TITLE = brush

# 生产环境配置
VITE_APP_ENV = 'production'

# 正确的后端API地址（包含/api/v1路径）
VITE_APP_BASE_API = '/api/v1'

# Vercel部署需要设置为根路径
VITE_APP_PUBLIC_PATH = '/'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
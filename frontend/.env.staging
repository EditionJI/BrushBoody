# 页面标题
VITE_APP_TITLE = brush

# 生产环境配置
VITE_APP_ENV = 'staging'

# 若依管理系统/生产环境
VITE_APP_BASE_API = '/asmalltoothbrushapplication/'

VITE_APP_PUBLIC_PATH = '/admin_web/'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip